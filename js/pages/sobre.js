// Sobre nós — só usa o layout compartilhado, não tem formulário/carrossel/lightbox.
import { initLayout } from '../layout.js';

document.addEventListener('DOMContentLoaded', () => {
  initLayout();
});
