// Contato — usa o layout compartilhado + os formulários (orçamento e
// trabalhe conosco).
import { initLayout } from '../layout.js';
import { initForms } from '../components/forms.js';

document.addEventListener('DOMContentLoaded', () => {
  initLayout();
  initForms();
});
